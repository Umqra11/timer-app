---
tags: [timer, daily, 2026-08, obsidian-ready]
created: 2026-08-07
updated: 2026-08-07 (S-0028 — Sprint-03 Faz 1+2+3 tamamlandı, canlıya deploy edildi. Faz 4 debug + polish ⏳.)
session: S-0022..S-0028
type: daily-note
---

# Daily Note — 2026-08-07

> 📚 **Bağlantılar:** [[00-Home]] · [[STATUS]] · [[RESUME]] · [[DECISIONS]]

---

## 🎯 Bugün Yapılan (Sprint-02 + Sprint-03)

### Sabah: Sprint-02 başlangıcı

**Karar: Vercel çıkarıldı, Firebase Hosting + Firestore = tek platform** (D-044)
- Enes "Vercel üzerinden değil de Firebase üzerinden yapsak?" diye sordu
- Trade-off'lar açıklandı: Vercel CDN biraz hızlı, ama Firebase tek konsol = daha az karışıklık
- Firebase Hosting'e geçiş onaylandı

### Sprint-02: Tamamlandı (S-0022)
- Vercel temizliği: `mobile/.vercel/`, `vercel.json`, `VERCEL_OIDC_TOKEN` silindi
- `@sveltejs/adapter-vercel` + `@sveltejs/adapter-auto` çıkar, `@sveltejs/adapter-static` kuruldu
- Firebase SDK + firebase-tools
- 6 Firebase dosyası (client, uid, usernames, rooms, presence, stats)
- 3 store (timer, rooms, username) Firestore'a bağlandı
- BroadcastChannel + Firestore onSnapshot hibrit (D-019)
- Tık sesi (D-017) + istatistikler (D-018)
- Username claim (D-016 server-side atomic transaction)
- Canlı: https://timerviber.web.app (proje: `timerviber`)

### Sprint-03 Faz 1: Oda temel (S-0024) ✅
- `routes/rooms/[id]/+page.svelte` — oda detay sayfası (geri, davet kodu, Odayı sil, onay modalı)
- D-048: `memberCount` denormalize sayaç (atomik increment)
- D-049: Oda sahibi silebilir (`deleteRoom()` + server-side owner check)
- `firestore.rules` güncellendi (rooms create/update/delete strict + presence match eklendi — kritik bug fix)
- Canlı doğrulama: 1 kişi → 2 kişi (ikinci katılımcı) → oda silme → Firestore 404

### Sprint-03 Faz 2: Leaderboard (S-0024..S-0026) 🟢
- `rooms.ts`: `subscribeRoomMembers` — N+1 query (her presence için `users/{uid}.totalSeconds`)
- D-050: `setRoomContext` + `visibilitychange` + `beforeunload` listener
- D-051: `effective` status — 2dk stale, 5dk finished-late
- Leaderboard UI: username + durum pill + toplam süre
- **KRİTİK BUG FIX (v6)**: `setRoomContext` artık `onRemote` callback olmadan da `writePresence()` çağırıyor. Önceki kod `if (!onRemote) return;` ile erken dönüyordu — bu yüzden presence hiç yazılmıyordu
- Canlıda kanıtlandı: v6 fix sonrası leaderboard'da `kullanici2` görünüyor, 0sn toplam süre, durum pill

### Sprint-03 Faz 3: Mesaj/Reactions (S-0027..S-0028) 🟡
- `src/lib/firebase/reactions.ts` (6.7 KB) — `sendReaction()` (D-052 atomic), `subscribeReactions()` (D-054)
- D-053: Rate limit (5/dk, 30/saat, sliding window)
- D-052: `expireAt` field — Firestore TTL policy (4 saat)
- D-055: Sadece kişiye özel tepki
- `+page.svelte`: Tepki yazma modalı (textarea + sayaç), leaderboard satırlarında baloncuklar
- `firestore.rules`: reactions match
- **KRİTİK FIX (v8)**: `runTransaction` async `getDoc` uyumsuz — sequential get+set'e geçildi
- Canlıda kanıtlandı: REST API ile yazılan reaction (`testus: test mesajı`) baloncuk olarak görünüyor. SDK `sendReaction` hâlâ 404 (debug gerek)

---

## 🔍 Keşifler (Bugün)

1. **Vercel zaten bağlıymış** — `mobile/.vercel/project.json` ve `vercel.json` dosyaları mevcuttu. RESUME "henüz deploy edilmedi" diyordu ama aslında Vercel projesi açılmıştı (`timer-app`). Yine de D-044 ile Vercel tamamen çıkarıldı.
2. **`firestore.rules` presence match'i unutulmuş** — İlk Sprint-03 deploy'da `allow read: if true` yazdık ama nested `match /presence/{uid}` yoktu. Collection read 403 dönüyordu. Eklendi, düzeldi.
3. **`runTransaction` + async `getDoc` uyumsuz** — `tx.get()` sadece aynı transaction'daki doc'ları okuyabilir, rate limit doc farklı collection'da. Sequential get+set'e geçildi.
4. **`setRoomContext.onRemote` opsiyonel olmalı** — Önceki kod `if (!onRemote) return;` ile erken dönüyordu, presence hiç yazılmıyordu. v6 fix sonrası leaderboard tam çalışıyor.

---

## 📊 Commit Özeti (Bugün)

```
14d49ce fix(reactions): sequential rate limit (runTransaction uyumsuz)
2c26c90 feat(sprint-03): reactions sistemi (D-052/053/054)
4ad33ef fix(sprint-03): make setRoomContext.onRemote truly optional  ← KRİTİK FIX
61daa83 fix(sprint-03): TickHandle type + $effect for room context
57e534b feat(sprint-03): oda detay sayfası, memberCount, owner delete, leaderboard
e62938a feat(sprint-02): Firebase integration, Firestore stores, stats, click sound
48f3098 chore(vercel): drop Vercel adapter, switch to adapter-static (D-044)
```

---

## ⏳ Sprint-04 — Yapılacaklar (Yeni Session'da)

### Debug (30-60 dk)
- [ ] SDK `sendReaction` neden 404 dönüyor? (rules field check, setDoc serialize, transaction scope)
- [ ] Firestore TTL policy Console'dan veya gcloud ile (`expireAt` field, 4 saat)
- [ ] Faz 2 v6 fix sonrası presence yazımı tam çalışıyor mu? (canlıda testus/kullanici2 ile doğrula)

### Server-side enforcement (1-2 saat)
- [ ] Cloud Function: `users/{uid}/rateLimit/reactions` server-side check
- [ ] Cloud Function: oda sahibi custom check (uid karşılaştırması, D-049 sıkılaştırma)
- [ ] Cloud Function: oda silme recursive delete (presence + reactions + joinedRooms)

### Polish (1 saat)
- [ ] Stats rolling week sum (D-018 — şu an sadece "bugün" gösteriliyor)
- [ ] Username reclamation policy (D-015 — eski username'ler orphan kalıyor)
- [ ] Console error monitoring

### Verification (30 dk)
- [ ] check + build + deploy
- [ ] Canlı smoke: 2 kullanıcı aynı odada, leaderboard + reactions
- [ ] RESUME/STATUS final güncelleme, commit & push

---

## 💡 Notlar

- **CDN cache trick**: Her deploy sonrası 30 saniye bekle, browser'a `?v=N` query string ile bypass et. Normal `location.reload()` eski build'i gösterebilir.
- **`gcloud` yok** — TTL policy için Firestore Console'dan manuel config gerekli.
- **Resim hâlâ eski** — Faz 2'de "kullanıcı 0sn toplam süre" görüyor çünkü daha timer başlatmadı. Bu normal, Sprint-04'te leaderboard doğrulaması yapılacak.

---

**Sonraki daily:** 2026-08-08 (Sprint-04 — debug + polish, Faz 1..3 final verification)
